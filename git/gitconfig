[user]
    name = Kaspars Mickevics
    email = kaspars@fx.lv
[color]
    diff = auto
    ui = auto
[push]
    default = simple
[alias]
    ls-deleted = ls-files --deleted
    diffc = diff --cached
    last = log --graph --relative-date --topo-order -n 10
    pretty = log --graph --topo-order --abbrev-commit --relative-date --decorate --all --boundary --pretty=format:'%Cgreen%ad %Cred%h%Creset -%C(yellow)%d%Creset %s %Cblue[%cn]%Creset'
    pretty-absolute = log --graph --topo-order --abbrev-commit --date=short --decorate --all --boundary --pretty=format:'%Cgreen%ad %Cred%h%Creset -%C(yellow)%d%Creset %s %Cblue[%cn]%Creset'

[credential]
	helper = cache --timeout=3600
